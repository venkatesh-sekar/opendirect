"use client"

import { GenerationsScreen } from "@/components/workspace/grids"

/** `/generations/` — Every run in the project, newest first, grouped by day. */
export default function Page() {
  return <GenerationsScreen />
}
