"use client"

import { ScenesScreen } from "@/components/workspace/grids"

/** `/scenes/` — Every scene in the project, as a grid of cards. */
export default function Page() {
  return <ScenesScreen />
}
