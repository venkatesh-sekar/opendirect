"use client"

import { CharactersScreen } from "@/components/workspace/grids"

/** `/characters/` — Every character in the project, as a grid of cards. */
export default function Page() {
  return <CharactersScreen />
}
