# OpenDirect

All notable changes to OpenDirect are documented here.

This file is generated by [changesets](https://github.com/changesets/changesets):
run `pnpm changeset` on every user-facing change and `pnpm changeset version` to
fold the pending changesets into the entries below and bump the app version.

## Unreleased

- Initial development. No public release yet.
