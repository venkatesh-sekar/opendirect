{
  "extends": "@workspace/typescript-config/base.json",
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    "declarationMap": false,
    "lib": ["es2023"],
    "types": []
  },
  "include": ["src/**/*.ts"],
  "exclude": [
    "node_modules",
    "dist",
    // These import the desktop provider adapters (Node APIs), so the root
    // tsconfig, which has Node types, checks them instead.
    "src/registry/check.test.ts",
    "src/registry/family-schema.test.ts",
    "src/registry/suggest.test.ts"
  ]
}
